import { registerTool } from "@/lib/tools/registry";

registerTool({
  slug: "radio",
  label: "Vehicle Radio",
  description: "STM32WL config record — LoRa parameters, pressure streams, RF chain timings.",
  icon: "settings_input_antenna",
});
