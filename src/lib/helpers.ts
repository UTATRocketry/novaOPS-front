// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

export function cssVar(token: string): string {
  return `var(--chakra-colors-${token.replace(/\./g, "-")})`;
}